<?php 
    // Update JSON with new users
    // $nextIndex = count($db['users']);
    // $db['users'][$nextIndex] = ['name' => $_POST['username'], 'password': $_POST['username'], 'elevated' => 0, ];
    // file_put_contents('db.json', json_encode($db));
?>